x = int(input("Enter a number: "))

print("Prime numbers up to", x, "are:")

for num in range(2, x + 1):
    is_prime = True
    for g in range(2, num):
        if num % g == 0:
            is_prime = False
            break
    if is_prime:
        print(num)
